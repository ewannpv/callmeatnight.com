name: Deploy

on:
    push:
        branches: ['main']

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Copy repository contents via scp
              uses: appleboy/scp-action@master
              with:
<<<<<<< HEAD
                  host: ${{ secrets.HOST }}
                  username: ${{ secrets.USERNAME }}
                  password: ${{ secrets.PASSWORD }}
                  port: ${{ secrets.PORT }}
                  source: 'admin/*,assets/*,index.html'
                  target: '/root/public/callmeatnight.com/'
=======
                host: ${{ secrets.HOST }}
                username: ${{ secrets.USERNAME }}
                password: ${{ secrets.PASSWORD }}                 
                port: ${{ secrets.PORT }}
                source: '.'
                target: '/root/public/callmeatnight.com/'
>>>>>>> 191ed2c0a6682c00574e799db4180335c3a1b26a
